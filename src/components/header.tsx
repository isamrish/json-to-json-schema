import React from "react";

const Header = () => {
  return (
    <header className="py-8 flex justify-between text-xl font-bold">
      <h2>JSON to JSON Schema</h2>
      <div>Light / Dark</div>
    </header>
  );
};

export default Header;
